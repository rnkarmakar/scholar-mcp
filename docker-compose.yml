services:
  scholar-mcp:
    build: .
    image: scholar-mcp:latest
    container_name: scholar-mcp
    restart: unless-stopped
    ports:
      - 8501:8501
      - 8502:8502
    volumes:
      - ./mcp_agent.secrets.yaml:/app/mcp_agent.secrets.yaml
